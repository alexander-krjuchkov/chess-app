services:
  engine:
    build: ./stockfish/service
    image: ${CONTAINER_REGISTRY_HOST:?error}/${ENGINE_IMAGE:?error}
    restart: unless-stopped
    environment:
      PORT: 5000

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod
    image: ${CONTAINER_REGISTRY_HOST:?error}/${BACKEND_IMAGE:?error}
    restart: unless-stopped
    environment:
      PORT: 8000
      BEST_MOVE_URL: http://engine:5000/best-move
      AUTH_CLIENT_ID: ${AUTH_CLIENT_ID:?error}
      AUTH_PROVIDER_ROOT_URL: ${AUTH_PROVIDER_ROOT_URL:?error}
      AUTH_PROVIDER_JWKS_URI: ${AUTH_PROVIDER_JWKS_URI:?error}

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    image: ${CONTAINER_REGISTRY_HOST:?error}/${FRONTEND_IMAGE:?error}
    restart: unless-stopped
    ports:
      - '80:80'
      - '443:443'
    environment:
      DOMAIN: ${DOMAIN:?error}
      VITE_AUTH_PROVIDER_ROOT_URL: ${AUTH_PROVIDER_ROOT_URL:?error}
      VITE_AUTH_CLIENT_ID: ${AUTH_CLIENT_ID:?error}
      VITE_AUTH_CLIENT_URL_ORIGIN: ${AUTH_CLIENT_URL_ORIGIN:?error}
      VITE_AUTH_CLIENT_REDIRECT_PATH: ${AUTH_CLIENT_REDIRECT_PATH:?error}
      VITE_AUTH_CLIENT_POST_LOGOUT_REDIRECT_PATH: ${AUTH_CLIENT_POST_LOGOUT_REDIRECT_PATH:?error}
    volumes:
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config:
